### **Задача 4. Документирование и верификация архитектурного решения**

**1. Команды терминала для получения контекста:**

```bash
mkdir -p docs/adr docs/development docs/requirements tests/requirements
```

**2. Блок для вставки полученного контекста:**

(Пользователь вставляет подтверждение создания директорий)

**3. Само задание:**

**Цель:** Зафиксировать решение, требования и правила сопровождения.

**Действия:**
- Создать `docs/adr/002-common-library-structure.md` в формате:
  ```markdown
  ## Context
  Дублирование моделей и утилит между scraping, API и quiz.
  ## Decision
  Создать common/ в monorepo. Альтернативы: submodules, копирование — отклонены.
  ## Status
  Accepted
  ## Consequences
  (+) Единая модель данных; (–) Обновления common требуют координации.
  ```
- Создать `docs/development/common-library-guidelines.md`:
  ```markdown
  ## Что можно в common:
  - Используется ≥2 сервисами
  - Не зависит от playwright/qdrant
  - Не содержит stateful-логики (например, singleton-менеджеры)
  ```
- Создать `docs/requirements/REQ-003-Common-Library.md` с ID, типом, требованием, обоснованием, критериями
- Создать `tests/requirements/test_req_003_common_library.py`:
  ```python
  def test_common_components_importable():
      from common.models.pydantic_models import ProblemResponse
      from common.utils.model_adapter import db_problem_to_problem
      from common.services.specification import get_specification
      assert ProblemResponse
  ```

**Acceptance Criteria:**
- Все три документа созданы по шаблонам
- Тест `test_req_003_common_library.py` проходит: `pytest -k test_req_003_common_library`