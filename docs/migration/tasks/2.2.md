### **Задача 2. Создание `common/` с установкой как редактируемого пакета**

**1. Команды терминала для получения контекста:**

```bash
# Создание структуры
mkdir -p common/{models,utils,processors,services,config}

# Копирование подтвержденных файлов (пример — только после подтверждения в Задаче 1)
# cp models/{problem_schema.py,database_models.py,pydantic_models.py} common/models/ 2>/dev/null || true
# ... (аналогично для других)
```

**2. Блок для вставки полученного контекста:**

(Пользователь вставляет *финальный список файлов из Задачи 1*, затем выполняет копирование)

**3. Само задание:**

**Цель:** Физически создать `common/` и обеспечить его установку через `pip install -e`.

**Действия:**
- Скопировать подтвержденные файлы в соответствующие поддиректории `common/`
- Создать `common/setup.py`:
  ```python
  from setuptools import setup, find_packages
  setup(
      name="ix-common",
      version="0.1.0",
      packages=find_packages(),
      python_requires=">=3.9",
  )
  ```
- Выполнить `pip install -e common/`
- Проверить импорт ключевых модулей

**Acceptance Criteria:**
- Все файлы из итогового списка перенесены
- `pip install -e common/` завершается успешно
- `python -c "from common.models.database_models import Problem; from common.utils.subject_mapping import get_subject_name; print('OK')"` — работает