### **Задача 1. Аудит кандидатов для `common/` с подтверждением независимости**

**1. Команды терминала для получения контекста:**

```bash
# Структура исходных директорий
ls models/ utils/ services/ processors/ config/

# Поиск импортов тяжелых зависимостей
grep -r "playwright\|qdrant_client\|BrowserManager\|Retriever" models/ utils/ services/ processors/ --include="*.py" | sort

# Проверка использования файлов в нескольких сервисах
grep -r "from models\." --include="*.py" . | cut -d: -f1 | sort | uniq -c | awk '$1 > 1 {print}'
grep -r "from utils.model_adapter\|from utils.subject_mapping\|from utils.task_id_utils\|from processors\." --include="*.py" . | cut -d: -f1 | sort | uniq -c | awk '$1 > 1 {print}'
```

**2. Блок для вставки полученного контекста:**

(Пользователь вставляет *результаты выполнения команд терминала* из п.1)

**3. Само задание:**

**Цель:** Точно определить, какие файлы кандидаты в `common/` действительно (а) используются в ≥2 местах и (б) не зависят от `playwright`/`qdrant`.

**Действия:**
- На основе вывода команд подтвердить или исключить каждый файл из списка кандидатов
- Сформировать финальный список файлов для переноса:
  - `models/`: `problem_schema.py`, `database_models.py`, `pydantic_models.py` — **только если не импортируют тяжелые зависимости**
  - `utils/`: `model_adapter.py`, `task_id_utils.py`, `subject_mapping.py`, `task_number_inferer.py`, `fipi_urls.py`, `downloader.py`, `metadata_extractor.py` — **только если не импортируют `BrowserManager`, `Retriever` и т.п.**
  - `processors/`: `page_processor.py`, `block_processor.py` — **только если не используют `playwright` напрямую**
  - `services/`: `specification.py` — **если не зависит от `qdrant`**
  - `config/`: `task_number_rules.json`

**Acceptance Criteria:**
- Сформирован итоговый список файлов для переноса
- Для каждого файла подтверждено: ≥2 использования + отсутствие тяжелых зависимостей
- Ни один файл с `playwright` или `qdrant` не включен